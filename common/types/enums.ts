export enum ProtocolType {
  COMPOUND_V2 = 'compound_v2',
  TEST_V1 = 'test_v1',
  TEST_V2 = 'test_v2',
}

export enum NetworkNameType {
  ETH = 'eth',
}

export enum NetworkType {
  MAINNET = 'mainnet',
  SEPOLIA = 'sepolia',
  ROPSTEN = 'ropsten',
  KOVAN = 'kovan',
  RINKEBY = 'rinkeby',
  GOERLI = 'goerli',
  LOCAL = 'local',
}
